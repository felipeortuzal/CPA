import { ArrowRight, BookOpen, CheckCircle2, Clock3, Flame, Target, TrendingUp } from 'lucide-react'
import { Badge } from '../components/ui/Badge'
import { Button } from '../components/ui/Button'
import { Card } from '../components/ui/Card'
import { Progress } from '../components/ui/Progress'
import { StatCard } from '../components/ui/StatCard'
import { dashboardMock as d } from '../features/dashboard/mock'

export function DashboardPage() {
  return <div className="mx-auto max-w-7xl space-y-6"><div className="flex flex-col gap-4 xl:flex-row xl:items-end xl:justify-between"><div><div className="mb-2 flex items-center gap-2"><Badge>CPA</Badge><span className="text-sm text-slate-500">Certificado Profissional ANBIMA</span></div><h1 className="text-3xl font-bold tracking-tight sm:text-4xl">Boa tarde, {d.name}.</h1><p className="mt-2 text-slate-500">Você está avançando bem. Continue priorizando os temas com menor cobertura.</p></div><Card className="min-w-[280px] bg-slate-950 text-white dark:bg-emerald-400 dark:text-slate-950"><p className="text-xs font-semibold uppercase tracking-[0.18em] opacity-60">Pronto para a prova</p><div className="mt-2 flex items-end gap-2"><span className="text-4xl font-black">61%</span><span className="mb-1 text-sm opacity-60">estimativa</span></div><p className="mt-2 text-xs opacity-60">Indicador orientativo, não é garantia de aprovação.</p></Card></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-5"><StatCard label="Progresso" value={`${d.progress}%`} helper="do programa coberto" icon={<TrendingUp className="h-5 w-5"/>}/><StatCard label="Questões" value={String(d.questions)} helper="respondidas" icon={<BookOpen className="h-5 w-5"/>}/><StatCard label="Taxa de acerto" value={`${d.accuracy}%`} helper="últimos 30 dias" icon={<CheckCircle2 className="h-5 w-5"/>}/><StatCard label="Horas estudadas" value={`${d.hours}h`} helper="tempo acumulado" icon={<Clock3 className="h-5 w-5"/>}/><StatCard label="Sequência" value={`${d.streak} dias`} helper="melhor: 11 dias" icon={<Flame className="h-5 w-5"/>}/></div><div className="grid gap-6 xl:grid-cols-[1.45fr_.85fr]"><Card><div className="mb-5 flex items-center justify-between"><div><h2 className="font-semibold">Progresso por macrotema</h2><p className="text-sm text-slate-500">Cobertura e desempenho por bloco do programa.</p></div><Button variant="ghost">Ver trilha <ArrowRight className="h-4 w-4"/></Button></div><div className="space-y-5">{d.topics.map((topic, i) => <div key={topic.name}><div className="mb-2 flex items-center gap-3"><span className="grid h-7 w-7 place-items-center rounded-lg bg-slate-100 text-xs font-bold dark:bg-white/10">{i+1}</span><div className="min-w-0 flex-1"><div className="flex justify-between gap-3"><p className="truncate text-sm font-medium">{topic.name}</p><span className="text-xs text-slate-500">{topic.progress}%</span></div></div></div><Progress value={topic.progress}/><div className="mt-1.5 flex justify-between text-xs text-slate-500"><span>Peso aproximado: {topic.weight}%</span><span>Acerto: {topic.accuracy}%</span></div></div>)}</div></Card><Card className="relative overflow-hidden bg-gradient-to-br from-emerald-50 to-white dark:from-emerald-400/[0.08] dark:to-white/[0.02]"><div className="absolute -right-12 -top-12 h-36 w-36 rounded-full bg-emerald-400/10 blur-2xl"/><p className="text-xs font-semibold uppercase tracking-[0.16em] text-emerald-600 dark:text-emerald-300">Continue estudando</p><h2 className="mt-3 text-xl font-bold">Produtos do mercado financeiro</h2><p className="mt-2 text-sm leading-6 text-slate-500">Retome do ponto em que parou e consolide o bloco de maior peso da prova.</p><div className="my-5"><div className="mb-2 flex justify-between text-xs"><span>Progresso do tema</span><span>29%</span></div><Progress value={29}/></div><Button className="w-full">Continuar agora <ArrowRight className="h-4 w-4"/></Button></Card></div><div className="grid gap-6 lg:grid-cols-2"><Card><div className="mb-4 flex items-center justify-between"><div><h2 className="font-semibold">Assuntos fracos</h2><p className="text-sm text-slate-500">Priorizados pelo seu desempenho.</p></div><Target className="h-5 w-5 text-rose-500"/></div><div className="space-y-2">{d.weak.map((item, i) => <div key={item} className="flex items-center justify-between rounded-xl border border-slate-200 p-3 dark:border-white/10"><div><p className="text-sm font-medium">{item}</p><p className="text-xs text-slate-500">Prioridade {i===0?'alta':'média'}</p></div><Button variant="secondary" className="px-3">Revisar</Button></div>)}</div></Card><Card><div className="mb-4"><h2 className="font-semibold">Últimos simulados</h2><p className="text-sm text-slate-500">Resultados recentes para acompanhar sua evolução.</p></div><div className="space-y-2">{d.simulations.map((s) => <div key={s.name+s.date} className="grid grid-cols-[1fr_auto_auto] items-center gap-3 rounded-xl border border-slate-200 p-3 dark:border-white/10"><div><p className="text-sm font-medium">{s.name}</p><p className="text-xs text-slate-500">{s.date}</p></div><span className="text-sm font-bold">{s.score}</span><span className={`rounded-full px-2 py-1 text-xs font-semibold ${s.passed?'bg-emerald-400/15 text-emerald-700 dark:text-emerald-300':'bg-rose-400/15 text-rose-700 dark:text-rose-300'}`}>{s.passed?'Aprovado':'Reprovado'}</span></div>)}</div></Card></div></div>
}
