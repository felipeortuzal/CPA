import { useState, type ReactNode } from 'react'
export function Tabs({ items }: { items: { label: string; content: ReactNode }[] }) { const [active, setActive] = useState(0); return <div><div className="mb-4 flex gap-2 border-b border-slate-200 dark:border-white/10">{items.map((item, index) => <button key={item.label} onClick={() => setActive(index)} className={`border-b-2 px-3 py-2 text-sm font-medium ${active === index ? 'border-emerald-400 text-slate-900 dark:text-white' : 'border-transparent text-slate-500'}`}>{item.label}</button>)}</div>{items[active]?.content}</div> }
